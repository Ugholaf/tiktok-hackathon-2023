query GetTransactionSummary($currency: Currency!, $sortOrder: SortOrder!, $fromDate: DateTime) {
  getTransactionSummary(currency: $currency, sortOrder: $sortOrder, fromDate: $fromDate) {
    amountIn
    amountOut
  }
}